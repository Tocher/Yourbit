'use strict';

angular.module('themes').controller('ThemeController', ['$scope', '$stateParams', '$location', 'Authentication', '$http', 
    function($scope, $stateParams, $location, Authentication,  $http) {
   		$scope.themes = [
   			
   		];
    }
]);